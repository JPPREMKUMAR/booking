{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS",
        "nixpacksPlan": {
            "phases": {
                "setup": {
                    "nixPkgs": [
                        "..."
                    ] // Optional, leave if no custom pkgs
                },
                "build": {
                    "cmds": [
                        "npm install --omit=dev"
                    ]
                }
            }
        }
    }
}